node_modules/
src-tauri/target/
src-tauri/gen/schemas/
# pnpm-lock.yaml is the authoritative lockfile for this package; the
# fallback path (npm install) produces package-lock.json — ignore it so
# the two lockfiles cannot coexist in source control.
package-lock.json
*.log
.DS_Store
