{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSeek Harness 桌面外壳：内嵌管理面板 + 内核版本管理与更新",
  "type": "module",
  "scripts": {
    "deps": "node scripts/install.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac-intel": "tauri build --target x86_64-apple-darwin",
    "build:win": "tauri build --target x86_64-pc-windows-msvc"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
